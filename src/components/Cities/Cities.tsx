// import { useQuery } from "@tanstack/react-query";
// import { СitiesQuery } from "../../features/СitiesQuery";

const Cities = () => {
  return (
    <div>
      {/* <СitiesQuery term={'Мос'}/> */}
    </div>
  );
};

export default Cities;